# pushkind-common

Shared utilities and models for Pushkind services.

This repository also exposes a placeholder npm package, `@pushkind/frontend-shell`,
from the repo root so React-migrated services can depend on a shared frontend
package via GitHub before common code is moved here.
